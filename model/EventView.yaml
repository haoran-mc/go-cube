cube:
  name: EventView
  sql_table: default.event

  segments:
    org:
      sql: ""

    expired:
      sql: ts + exp < now()

  dimensions:
    ts:
      sql: ts
      type: time
      title: 时间

    risk:
      sql: risk
      type: string
      title: 风险

    content:
      sql: content
      type: string
      title: 内容

    level:
      sql: level
      type: string
      title: 级别

    data:
      sql: data
      type: string
      title: 数据

    channel:
      sql: channel
      type: string
      title: 渠道

    desc:
      sql: dictGet('default.risk_dict', 'desc', risk)
      type: string
      title: 告警描述

  measures:
    count:
      sql: count(risk)
      type: number
      title: 数量

    firstTs:
      sql: min(ts)
      type: time
      title: 首次告警时间

    lastTs:
      sql: max(ts)
      type: time
      title: 最后告警时间
